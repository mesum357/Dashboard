<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tourist Information Receipt</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f8f9fa;
            margin: 0;
            padding: 5px;
            color: #2c3e50;
        }

        .receipt-container {
            max-width: 800px;
            margin: 0 auto;
            background: white;
            border: 1px solid #dee2e6;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
        }

        /* Official Header */
        .official-header {
            background: #2c3e50;
            color: white;
            padding: 8px;
            text-align: center;
            border-bottom: 2px solid #e74c3c;
        }

        .department-logo {
            font-size: 16px;
            font-weight: bold;
            margin-bottom: 2px;
            letter-spacing: 1px;
        }

        .department-name {
            font-size: 12px;
            font-weight: 600;
            margin-bottom: 2px;
        }

        .document-title {
            font-size: 14px;
            font-weight: bold;
            text-transform: uppercase;
            letter-spacing: 1px;
            margin: 4px 0;
            border: 1px solid white;
            padding: 4px 8px;
            display: inline-block;
        }

        .receipt-number {
            font-size: 10px;
            margin-top: 4px;
            opacity: 0.9;
        }

        /* Receipt Body */
        .receipt-body {
            padding: 10px;
        }

        /* Information Sections */
        .info-section {
            margin-bottom: 8px;
            border: 1px solid #e9ecef;
            border-radius: 3px;
            overflow: hidden;
        }

        .section-header {
            background: #f8f9fa;
            padding: 4px 8px;
            font-weight: bold;
            color: #2c3e50;
            border-bottom: 1px solid #e9ecef;
            font-size: 11px;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .section-content {
            padding: 8px;
        }

        /* Image Grid */
        .image-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 8px;
            margin-bottom: 8px;
        }

        .image-container {
            text-align: center;
            border: 1px solid #e9ecef;
            border-radius: 3px;
            padding: 5px;
            background: #f8f9fa;
        }

        .image-container img {
            width: 100%;
            max-width: 80px;
            height: 50px;
            object-fit: cover;
            border: 1px solid #dee2e6;
            border-radius: 2px;
            margin-bottom: 3px;
        }

        .image-label {
            font-weight: bold;
            color: #495057;
            font-size: 10px;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        /* Information Table */
        .info-table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 5px;
        }

        .info-table tr {
            border-bottom: 1px solid #e9ecef;
        }

        .info-table tr:last-child {
            border-bottom: 2px solid #2c3e50;
        }

        .info-table td {
            padding: 4px 6px;
            vertical-align: top;
            font-size: 11px;
        }

        .info-table .label {
            font-weight: bold;
            color: #2c3e50;
            width: 40%;
            background: #f8f9fa;
            border-right: 1px solid #e9ecef;
        }

        .info-table .value {
            color: #495057;
            font-weight: 500;
        }

        /* Official Footer */
        .official-footer {
            background: #f8f9fa;
            padding: 8px;
            border-top: 2px solid #2c3e50;
            text-align: center;
        }

        .signature-section {
            display: flex;
            justify-content: space-between;
            margin-top: 8px;
            padding-top: 8px;
            border-top: 1px solid #e9ecef;
        }

        .signature-box {
            text-align: center;
            flex: 1;
            margin: 0 5px;
        }

        .signature-line {
            width: 80px;
            height: 1px;
            background: #2c3e50;
            margin: 12px auto 4px auto;
        }

        .signature-label {
            font-weight: bold;
            color: #2c3e50;
            font-size: 10px;
            text-transform: uppercase;
        }

        .stamp-area {
            text-align: center;
            margin-top: 8px;
            padding: 6px;
            border: 1px dashed #adb5bd;
            border-radius: 3px;
            background: #ffffff;
        }

        .stamp-text {
            color: #6c757d;
            font-style: italic;
            font-size: 10px;
        }

        /* Action Buttons */
        .action-buttons {
            text-align: center;
            margin: 10px 0;
            padding: 10px;
            background: #f8f9fa;
            border-radius: 6px;
        }

        .action-buttons button {
            padding: 8px 16px;
            margin: 0 6px;
            border: none;
            border-radius: 6px;
            cursor: pointer;
            font-weight: 600;
            font-size: 12px;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            transition: all 0.3s ease;
        }

        .download-btn {
            background: #2c3e50;
            color: white;
        }

        .download-btn:hover {
            background: #34495e;
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(44, 62, 80, 0.3);
        }

        .print-btn {
            background: #e74c3c;
            color: white;
        }

        .print-btn:hover {
            background: #c0392b;
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(231, 76, 60, 0.3);
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            .receipt-container {
                margin: 10px;
                border-width: 1px;
            }

            .receipt-body {
                padding: 20px;
            }

            .image-grid {
                grid-template-columns: 1fr;
            }

            .signature-section {
                flex-direction: column;
                gap: 20px;
            }

            .signature-box {
                margin: 0;
            }

            .info-table .label,
            .info-table .value {
                display: block;
                width: 100%;
                border-right: none;
                border-bottom: 1px solid #e5e7eb;
            }
        }

        /* Print Styles */
        @media print {
            /* Hide everything except the receipt */
            body * {
                visibility: hidden;
            }
            
            .receipt-container, .receipt-container * {
                visibility: visible;
            }
            
            body {
                background: white;
                padding: 0;
                margin: 0;
            }

            .receipt-container {
                position: absolute;
                top: 0;
                left: 0;
                width: 100%;
                box-shadow: none;
                border: 1px solid #2c3e50;
                max-width: none;
                margin: 0;
            }

            .action-buttons {
                display: none;
            }

            .official-header {
                padding: 10px;
            }

            .department-logo {
                font-size: 16px;
                margin-bottom: 3px;
            }

            .department-name {
                font-size: 14px;
                margin-bottom: 3px;
            }

            .document-title {
                font-size: 14px;
                margin: 5px 0;
                padding: 5px 10px;
            }

            .receipt-number {
                font-size: 10px;
                margin-top: 5px;
            }

            .receipt-body {
                padding: 10px;
            }

            .info-section {
                margin-bottom: 8px;
            }

            .section-header {
                padding: 5px 10px;
                font-size: 12px;
            }

            .section-content {
                padding: 8px;
            }

            .image-grid {
                gap: 8px;
                margin-bottom: 8px;
                grid-template-columns: repeat(3, 1fr);
            }

            .image-container {
                padding: 5px;
            }

            .image-container img {
                max-width: 80px;
                height: 50px;
            }

            .image-label {
                font-size: 10px;
            }

            .info-table {
                margin-top: 5px;
            }

            .info-table td {
                padding: 4px 6px;
                font-size: 11px;
            }

            .official-footer {
                padding: 8px;
            }

            .signature-section {
                margin-top: 8px;
                padding-top: 8px;
            }

            .signature-box {
                margin: 0 5px;
            }

            .signature-line {
                width: 80px;
                margin: 12px auto 4px auto;
            }

            .signature-label {
                font-size: 10px;
            }

            .stamp-area {
                margin-top: 8px;
                padding: 8px;
            }

            .stamp-text {
                font-size: 10px;
            }

            @page {
                size: A4;
                margin: 8mm;
            }
        }

        /* Watermark effect */
        .receipt-container::before {
            content: "OFFICIAL DOCUMENT";
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%) rotate(-45deg);
            font-size: 48px;
            color: rgba(44, 62, 80, 0.05);
            font-weight: bold;
            pointer-events: none;
            z-index: -1;
        }

        /* Subadmin sidebar styles */
        .subadmin-sidebar {
            width: 250px;
            position: fixed;
            left: 0;
            top: 0;
            height: 100vh;
            background: #081c3a;
            color: white;
            z-index: 100;
        }

        .main-content-with-subadmin {
            padding: 20px;
            width: calc(100% - 250px);
            box-sizing: border-box;
            margin-left: 250px;
        }

        @media (max-width: 768px) {
            .subadmin-sidebar {
                width: 180px;
            }
            .main-content-with-subadmin {
                width: calc(100% - 180px);
                margin-left: 180px;
            }
        }
    </style>
    <!-- Include html2canvas and jsPDF libraries for PDF generation -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/html2canvas/0.4.1/html2canvas.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jspdf/2.4.0/jspdf.umd.min.js"></script>
    <!-- Include html2pdf library for PDF generation -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/html2pdf.js/0.9.2/html2pdf.bundle.min.js"></script>
</head>
<body>
    <% if (value === 1) { %>
        <%- include('sidebar') %>
        <div class="main-content" style="margin: 0;">
    <% } else if (value === 2) { %>
        <div class="subadmin-sidebar">
            <%- include('subadmin-sidebar') %>
        </div>
        <div class="main-content-with-subadmin" style="margin: 0;">
    <% } %>
        <div class="action-buttons">
            <button class="download-btn" onclick="downloadPDF()">Download PDF</button>
            
        </div>
        
        <div class="receipt-container" id="receipt">
            <!-- Official Header -->
            <div class="official-header">
                <div class="department-logo">GILGIT BALTISTAN</div>
                <div class="department-name">TOURIST POLICE DEPARTMENT</div>
                <div class="document-title">Tourist Information Record</div>
                <div class="receipt-number">Document No: <%= data.orderNo || 'N/A' %></div>
            </div>

            <!-- Receipt Body -->
            <div class="receipt-body">
                <!-- Images Section -->
                <div class="info-section">
                    <div class="section-header">Supporting Documents</div>
                    <div class="section-content">
                        <div class="image-grid">
                            <div class="image-container">
                                <span class="image-label">Profile Image</span>
                                <% if(data.profileImage){ %>
                                    <img src="/<%= data.profileImage.replace(/\\/g, '/') %>" alt="Profile Image">
                                <% } else { %>
                                    <div style="height: 50px; background: #f8f9fa; display: flex; align-items: center; justify-content: center; color: #6c757d; font-style: italic; font-size: 9px;">No Image</div>
                                <% } %>
                            </div>
                            <div class="image-container">
                                <span class="image-label">CNIC Image</span>
                                <% if(data.vehicleImage){ %>
                                    <img src="/<%= data.vehicleImage.replace(/\\/g, '/') %>" alt="CNIC Image">
                                <% } else { %>
                                    <div style="height: 50px; background: #f8f9fa; display: flex; align-items: center; justify-content: center; color: #6c757d; font-style: italic; font-size: 9px;">No Image</div>
                                <% } %>
                            </div>
                            <div class="image-container">
                                <span class="image-label">Driving License</span>
                                <% if(data.drivingLicenseImage){ %>
                                    <img src="/<%= data.drivingLicenseImage.replace(/\\/g, '/') %>" alt="Driving License">
                                <% } else { %>
                                    <div style="height: 50px; background: #f8f9fa; display: flex; align-items: center; justify-content: center; color: #6c757d; font-style: italic; font-size: 9px;">No Image</div>
                                <% } %>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Basic Information Section -->
                <div class="info-section">
                    <div class="section-header">Basic Information</div>
                    <div class="section-content">
                        <table class="info-table">
                            <tr>
                                <td class="label">Location</td>
                                <td class="value"><%= data.location %></td>
                            </tr>
                            <tr>
                                <td class="label">Date</td>
                                <td class="value"><%= data.orderNo %></td>
                            </tr>
                            <tr>
                                <td class="label">Time</td>
                                <td class="value"><%= data.time %></td>
                            </tr>
                            <tr>
                                <td class="label">CNIC Number</td>
                                <td class="value"><%= data.cnic %></td>
                            </tr>
                        </table>
                    </div>
                </div>

                <!-- Vehicle Information Section -->
                <div class="info-section">
                    <div class="section-header">Vehicle Information</div>
                    <div class="section-content">
                        <table class="info-table">
                            <tr>
                                <td class="label">Vehicle with Passengers</td>
                                <td class="value"><%= data.vehiclePassengers %></td>
                            </tr>
                            <tr>
                                <td class="label">Vehicle with Goods</td>
                                <td class="value"><%= data.vehicleGoods %></td>
                            </tr>
                            <tr>
                                <td class="label">Goods Description</td>
                                <td class="value"><%= data.goods %></td>
                            </tr>
                        </table>
                    </div>
                </div>

                <!-- Passenger Information Section -->
                <div class="info-section">
                    <div class="section-header">Passenger Information</div>
                    <div class="section-content">
                        <table class="info-table">
                            <tr>
                                <td class="label">Local Passengers</td>
                                <td class="value"><%= data.passengers %></td>
                            </tr>
                            <tr>
                                <td class="label">Males</td>
                                <td class="value"><%= data.males %></td>
                            </tr>
                            <tr>
                                <td class="label">Females</td>
                                <td class="value"><%= data.females %></td>
                            </tr>
                            <% if (typeof data.foreigners !== 'undefined' && data.foreigners != 0) { %>
                                <tr>
                                    <td class="label">Foreigner Passengers</td>
                                    <td class="value"><%= typeof data.foreigners !== 'undefined' ? data.foreigners : 'N/A' %></td>
                                </tr>
                                <% if (typeof data.foreignMales !== 'undefined') { %>
                                <tr>
                                    <td class="label">Foreign Males</td>
                                    <td class="value"><%= data.foreignMales %></td>
                                </tr>
                                <% } %>
                                <% if (typeof data.foreignFemales !== 'undefined') { %>
                                <tr>
                                    <td class="label">Foreign Females</td>
                                    <td class="value"><%= data.foreignFemales %></td>
                                </tr>
                                <% } %>
                            <% } %>
                        </table>
                    </div>
                </div>
            </div>

            <!-- Official Footer -->
            <div class="official-footer">
                <div class="signature-section">
                    <div class="signature-box">
                        <div class="signature-line"></div>
                        <div class="signature-label">Authorized Officer</div>
                    </div>
                    <div class="signature-box">
                        <div class="signature-line"></div>
                        <div class="signature-label">Supervisor</div>
                    </div>
                    <div class="signature-box">
                        <div class="signature-line"></div>
                        <div class="signature-label">Station Head</div>
                    </div>
                </div>
                
                <div class="stamp-area">
                    <div class="stamp-text">Official Stamp & Seal Area</div>
                </div>
                
                <div style="margin-top: 8px; font-size: 9px; color: #6c757d;">
                    <strong>Note:</strong> This is an official document of the Gilgit Baltistan Tourist Police Department. 
                    Any unauthorized reproduction or alteration is strictly prohibited.
                </div>
            </div>
        </div>
    </div>
    <% if (value === 2) { %>
        </div>
    <% } %>
    <script>
        function printReceipt() {
            window.print();
        }
        
        function downloadPDF() {
            const element = document.getElementById('receipt');
            const opt = {
                margin: [10, 10, 10, 10],
                filename: 'tourist_info_receipt.pdf',
                image: { type: 'jpeg', quality: 0.98 },
                html2canvas: { 
                    scale: 2,
                    useCORS: true,
                    allowTaint: true
                },
                jsPDF: { 
                    unit: 'mm', 
                    format: 'a4', 
                    orientation: 'portrait' 
                }
            };
            
            html2pdf().set(opt).from(element).save();
        }
        
        // Add some professional touches
        document.addEventListener('DOMContentLoaded', function() {
            // Add timestamp to the receipt
            const timestamp = new Date().toLocaleString('en-US', {
                year: 'numeric',
                month: '2-digit',
                day: '2-digit',
                hour: '2-digit',
                minute: '2-digit',
                second: '2-digit'
            });
            
            // You can add more professional features here
            console.log('Official Police Department Receipt loaded at:', timestamp);
        });
    </script>
</body>
</html>